# Email Helper - Consolidated Dependencies
# Optimized for maintainability and minimal dependencies

# Core dependencies
pandas>=2.0.0                   # Data processing and CSV operations
pywin32>=306                    # Outlook COM integration - provides pythoncom, win32com, pywintypes (Windows only)
python-dotenv>=1.0.0            # Environment variable management
pydantic>=2.0.0                 # Data validation and settings management
pydantic-settings>=2.0.0        # Settings management using pydantic (used in backend/core/config.py)

# Backend API
fastapi>=0.100.0                # FastAPI web framework for backend API
uvicorn[standard]>=0.23.0       # ASGI server for FastAPI

# Azure OpenAI integration  
openai>=1.3.0                  # Azure OpenAI client library
prompty>=0.1.50                # Microsoft Prompty for AI prompt management (1.0.0 not yet released)
azure-identity>=1.15.0         # Azure authentication using DefaultAzureCredential

# Optional: Enhanced text processing (can be removed if not used)
# nltk>=3.8                     # Natural language processing
# scikit-learn>=1.3.0          # Machine learning for classification enhancement

# Visualization
matplotlib>=3.7.0              # Chart visualization for accuracy dashboard

# Real-time updates
websockets>=12.0               # Real-time WebSocket communication

# Optional: Background processing (not currently in use - commented out in job_queue.py)
# celery>=5.3.0                  # Background job processing
# redis>=5.0.0                   # Job queue and caching

# Development and testing
pytest>=7.0.0                  # Testing framework
pytest-mock>=3.10.0           # Enhanced mocking capabilities
pytest-cov>=4.0.0             # Coverage reporting
pytest-asyncio>=0.21.0        # Async testing support
black>=23.0.0                  # Code formatting (optional)
